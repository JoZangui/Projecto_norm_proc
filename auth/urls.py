# auth/urls.py
""" URLs relacionadas à autenticação OIDC personalizada.
Define endpoints para iniciar o fluxo de autenticação e lidar com callbacks.
"""
# ! retornar quando for necessário o OIDC

# from django.urls import path
# from users_app.views import (
#     CustomOIDCAuthenticationRequestView,
#     CustomOIDCAuthenticationCallbackView,
# )

# urlpatterns = [
#     # URL para iniciar o fluxo de autenticação OIDC
#     path("oidc/authenticate/", CustomOIDCAuthenticationRequestView.as_view(), name="oidc_authentication_init"),
#     # Callback URL após autenticação OIDC
#     path("oidc/callback/", CustomOIDCAuthenticationCallbackView.as_view(), name="oidc_callback"),
# ]
